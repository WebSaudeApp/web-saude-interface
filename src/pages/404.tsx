import PagePlaceholder from "@/components/common/PagePlaceholder";

export default function NotFoundPage() {
  return (
    <PagePlaceholder
      title="Página não encontrada"
      description="A rota pedida não existe neste projeto."
    />
  );
}
