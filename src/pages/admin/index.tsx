import PagePlaceholder from "@/components/common/PagePlaceholder";

export default function AdminPage() {
  return (
    <PagePlaceholder
      title="Painel admin"
      description="Indicadores, aprovações pendentes e gestão de usuários."
    />
  );
}
